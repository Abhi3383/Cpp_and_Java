#include <bits/stdc++.h>
using namespace std;

class Complex
{
    int a, b;
    // name of members are a,b,set,showData,add,complex.
public:
    Complex()
    {
        cout << "HELLO WORLD" << endl;
    }
};

int main()
{
    Complex c1, c2, c3;
    // o/p shows that as soon as the object is created constructor calls itself(implici calling)
    // 3 object 3 o/p
    return 0;
}